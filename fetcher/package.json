{
  "name": "fetcher",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "tsc": "tsc",
    "start": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "geolib": "^3.3.4",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250129.0",
    "typescript": "5.7.3",
    "wrangler": "^3.107.0"
  }
}
